import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
  palette: {
    primary: {
      main: '#22F81E',
    },
    warning: {
      main: '#FF0202',
    },
  }
});

export default customTheme;